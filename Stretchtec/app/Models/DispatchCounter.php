<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Model;

/**
 * --------------------------------------------------------------------------
 * DispatchCounter Model
 * --------------------------------------------------------------------------
 * Represents the `dispatch_counters` table in the database.
 *
 * ✅ Key Points:
 *  - Currently an empty model extending Eloquent's base `Model`.
 *  - Used as a placeholder to interact with the database table.
 *  - Can later include:
 *      • Mass-assignable attributes (`$fillable`)
 *      • Attribute casting (`$casts`)
 *      • Relationships (belongsTo, hasMany, etc.)
 *      • Business logic specific to dispatch counters
 *
 * --------------------------------------------------------------------------
 */
class DispatchCounter extends Model
{
    // Placeholder: Add fillable fields, casts, or relationships here when needed.
}
